{
  "name": "zerotrust-agents",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*",
    "ee/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "latest"
  },
  "packageManager": "npm@10.8.0",
  "dependencies": {
    "@tailwindcss/postcss": "^4.3.0"
  }
}
